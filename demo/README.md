# Synthetic CLI demonstration

`recording.csv`, `expected_intervals.json`, and `answers.txt` are generated by
`python scripts/make_demo.py` followed by the documented `--no-slm` command.
They are synthetic smoke-test artifacts, not ExtraSensory observations and not
hackathon accuracy results. The committed answers use the transparent fallback
because no real-data recognizer is committed yet.
